import CreateCategory from '@/components/CreateCategory';
import React from 'react';

const page = () => {
    return (
        <div>
            <CreateCategory/>
        </div>
    );
};

export default page;